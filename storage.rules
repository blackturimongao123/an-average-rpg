rules_version = '2';

// MVP: Storage is enabled but unused. Open paths when avatars/exports/assets ship.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
